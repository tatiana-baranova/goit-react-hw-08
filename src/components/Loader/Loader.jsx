import { RiseLoader } from "react-spinners";

const Loader = () => {
    return (
        <RiseLoader
        color="#e6f5fa"
        loading="loading"
        margin={1}
        size={10}
        speedMultiplier={2}
/>
    )
}
export default Loader;